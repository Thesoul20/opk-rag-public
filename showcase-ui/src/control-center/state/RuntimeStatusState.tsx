import { createContext,useCallback,useContext,useEffect,useMemo,useState } from "react";import { fetchRuntimeStatus,type RuntimeStatusPayload } from "../api/runtimeStatus";
type S={status?:RuntimeStatusPayload;loading:boolean;error?:string};const C=createContext<{state:S;refresh:()=>Promise<void>}|null>(null);
export function RuntimeStatusProvider({children,loader=fetchRuntimeStatus}:{children:React.ReactNode;loader?:()=>Promise<RuntimeStatusPayload>}){const [state,setState]=useState<S>({loading:true});const refresh=useCallback(async()=>{setState(s=>({...s,loading:true,error:undefined}));try{const status=await loader();setState({status,loading:false});}catch(e){setState({loading:false,error:e instanceof Error?e.message:"runtime status unavailable"});}},[loader]);useEffect(()=>{void refresh()},[refresh]);const value=useMemo(()=>({state,refresh}),[state,refresh]);return <C.Provider value={value}>{children}</C.Provider>}
export function useRuntimeStatus(){const v=useContext(C);if(!v) throw new Error("useRuntimeStatus must be used within RuntimeStatusProvider");return v}
